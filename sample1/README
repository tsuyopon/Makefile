# 概要
Makefileの単純なサンプル
以下のような単純なサンプルです。
- str.hやmain.ccでコンパイルされたものが最新であれば何もしない 
- str.hまたはmain.ccを修正するとgmakeすると最新を生成する 

### 確認してほしいこと
- gmakeを実行し、mainバイナリが生成されることを確認する(出力は2行) 
- 再度gmakeを実行すると、何も再生されないことを確認する(出力は"gmake: Nothing to be done for `all'."などと表示) 
- str.hを適当に修正(改行やスペースを適当に入れる)して、再度gmakeを実行すると.oからバイナリへの変換のみ行うことを確認する(出力は1行) 
- main.cを適当に修正して、gmakeを実行すると.oファイル生成〜mainバイナリ生成までを行うことを確認する(出力は2行) 

